{
  "name": "python-field-control-system",
  "version": "1.0.0",
  "description": "A system for managing Python code execution on Raspberry Pi controlled field",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "pi": "cd raspberry-pi && python app.py",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../raspberry-pi && pip install -r requirements.txt"
  },
  "keywords": ["python", "raspberry-pi", "field-control", "queue-management"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
